import React from 'react';
;

export interface TodoEditorProps {
  className?: string;
  style?: React.CSSProperties;
}

export const TodoEditor: React.FC<TodoEditorProps> = ({className, style}) => {
  return <div className="">content</div>;
}
